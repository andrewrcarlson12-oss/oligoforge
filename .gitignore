# Build artifacts (regenerated by PyInstaller / CI — never commit these)
build/
dist/
*.egg-info/
__pycache__/
*.py[cod]

# OligoForge runtime data (per-machine, not source)
panels/
projects/

# Local environments / secrets
.env
.venv/
venv/

# OS + editor cruft
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
